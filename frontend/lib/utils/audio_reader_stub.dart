import 'dart:typed_data';

/// Stub implementation — should never be called directly.
/// Conditional imports will replace this with the correct platform version.
Future<Uint8List> readRecordedAudioBytes(String path) async {
  throw UnsupportedError('Cannot read audio on this platform');
}
